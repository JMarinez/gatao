import 'package:flutter/material.dart';

class AccountWalletDetail extends StatelessWidget {
  const AccountWalletDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
